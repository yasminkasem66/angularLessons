import { ValidationErrors } from "@angular/forms";

export class PasswordValidate {

    // static checkPassword(control: AbstractRange): ValidationErrors | null{

    //     if ((control.value as string) == '1234') {
    //       return { checkPassword: true };
    //     }
        
    // }
}