// we can use  document.querySelector(#footer).scrollIntoView() to scroll the element.
//  we can use { behavior: 'smooth' } to scroll smoothly.
document.querySelector("#footer").scrollIntoView({
  behavior: "smooth",
});
